export const width = 960;
export const height = 640;

export const center = { x: width / 2, y: height / 2 };

export const partyCenters = {
  democrat: { x: width / 4, y: height / 2 },
  both: { x: 2 * width / 4, y: height / 2 },
  republican: { x: (3 * width) / 4, y: height / 2 }
};
